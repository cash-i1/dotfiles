# dotfiles
## requirements
pacman packages:
```bash
sudo pacman -S \
    ttf-jetbrains-mono \
    unzip \
    git \
    fish \
    npm \
    ripgrep \ # optional (for telescope)
    stow \ # optional (for symlinking dotfiles)

```
